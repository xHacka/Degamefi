import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `'PixelSplitter', sans-serif`,
    body: `'PixelSplitter', sans-serif`,
  },
});

export default theme;
